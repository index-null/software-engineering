# PostgreSQL Client Authentication Configuration File

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow all local connections
local   all             all                                     trust

# Allow connections from your specific IP range or all
host    all             all             172.23.32.1/24         md5
# Or allow all external connections (not recommended for production)
# host    all             all             0.0.0.0/0               md5

# Example for IPv6
host    all             all             ::1/128                md5
